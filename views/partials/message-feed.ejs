<ul class="uk-list" id="message-list">
  <% messages.forEach((item) => { %>
      <li class="messasge">
      <div>
        <span class="title"><%= item.content %></span>
        <p>@<%= item.originId %></p>
      </div>
      </li>
  <% }) %>

</ul>
